<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.MemberDAO"><!-- TodoDAO를 매핑해서 아래의 select id = selectAll을 쓴다 -->



	<!-- 멤버 -->
	<select id="currentMember" resultType="MemberVO"
		parameterType="MemberVO">
		select *
		from t_member
		where id = #{id} and password = #{password} <!-- -->
	</select>


	<!-- 회원가입 -->
	<insert id="InsertMember" parameterType="MemberVO">
   insert into t_member(id, password, name, email)
   values(#{id}, #{password}, #{name}, #{email})
</insert>
	
	<select id="checkId" parameterType="string">
    SELECT COUNT(*) FROM t_member WHERE id = #{id}
	</select>

	

</mapper>