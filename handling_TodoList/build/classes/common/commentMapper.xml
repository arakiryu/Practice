<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="dao.CommentDAO">

	<!-- 게시글에 대한 댓글 조회 -->
	<select id="selectCmtBoardNo" resultType="CommentVO">
		SELECT 
    comment_no as cmtNo,
    board_id,
    member_id,
    parent,
    depth,
    content,
    created_at
	FROM t_comment
	WHERE board_id = #{board_id}
	ORDER BY created_at ASC
	</select>

	<!-- 최상위 댓글 등록 -->
	<insert id="insertBoardParentCmt" parameterType="CommentVO">
		INSERT INTO t_comment (comment_no, board_id, member_id, content)
		VALUES (comment_seq.nextval , #{board_id} , #{member_id}, #{content, jdbcType=CLOB})
	</insert>

	<!-- 자식 댓글 등록 -->
	<insert id="insertBoardChildCmt" parameterType="CommentVO">
    INSERT INTO t_comment (
        comment_no, board_id, member_id, 
        parent, depth, content, created_at
    )
    VALUES (
        comment_seq.nextval, 
        #{board_id}, 
        #{member_id}, 
        #{parent},
        (SELECT depth + 1 FROM t_comment WHERE comment_no = #{parent}),
        #{content},
        SYSTIMESTAMP
    )
</insert>
	
	<!-- 댓글 수정 -->
	<update id="updateCmt" parameterType="CommentVO">
		UPDATE 
			t_comment
		SET 
			content = #{content}
			created_at = SYSTIMESTAMP
		WHERE comment_no = #{comment_no}
		and member_id = #{member_id}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteCmt" parameterType="int">
		DELETE FROM t_comment
		WHERE comment_no = #{comment_no}
	</delete>
	
	
	<!-- 댓글 한번에 삭제 -->
 <delete id="deleteAllByBoardId" parameterType="string">
    DELETE FROM t_comment 
    WHERE board_id = #{boardId}
</delete>
	
</mapper>