<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
 PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
  <mapper namespace="dao.BoardDAO">

	<!-- 전체 게시글 조회 -->
	<select id="selectAll" resultType="BoardVO">
		SELECT board_id as boardId,
		title,
		content,
		member_id as memberId,
		created_at as createdAt,
		views
		FROM t_board
		ORDER BY board_id DESC
	</select>

	<!-- 특정 게시글 조회 -->
	<select id="selectByBoardId" parameterType="int"
		resultType="BoardVO">
		SELECT board_id as boardId,
		title,
		content,
		member_id as memberId,
		created_at as createdAt,
		views
		FROM t_board
		WHERE board_id = #{boardId}
	</select>

	<!-- 게시글 등록 -->
	<insert id="insertBoard" parameterType="BoardVO">
		INSERT INTO t_board (title, content, member_id)
		VALUES (#{title}, #{content}, #{memberId})
	</insert>

	<!-- 게시글 수정 -->
	<update id="updateBoard" parameterType="BoardVO">
		UPDATE t_board
		SET title = #{title},
		content = #{content}
		WHERE board_id = #{boardId}
	</update>

	<!-- 게시글 삭제 -->
	<delete id="deleteBoard" parameterType="int">
		DELETE FROM t_board
		WHERE board_id = #{boardId}
	</delete>

	<!-- 조회수 증가 -->
	<update id="incrementViews" parameterType="int">
		UPDATE t_board
		SET views = views + 1
		WHERE board_id = #{boardId}
	</update>

	<!-- 특정 회원의 게시글 목록 조회 -->
	<select id="selectByMemberId" parameterType="string"
		resultType="BoardVO">
		SELECT board_id as boardId,
		title,
		content,
		member_id as memberId,
		created_at as createdAt,
		views
		FROM t_board
		WHERE member_id = #{memberId}
		ORDER BY board_id DESC
	</select>

	<!-- 특정 갯수 조회 -->
	<select id="selectviewPage" parameterType="map" resultType="BoardVO">
	SELECT board_id as boardId,
           title,
           content,
           member_id as memberId,
           created_at as createdAt,
           views
    FROM (
        SELECT ROWNUM as RN, board_id, title, content, member_id, created_at, views
        FROM t_board 
        ORDER BY board_id DESC
    )
    WHERE RN BETWEEN #{startNo} AND #{endNo}
   </select>
</mapper>